@media (prefers-reduced-motion: no-preference) {
  html { 
    interpolate-size: allow-keywords; /* Permite animaÃ§Ã£o de tamanhos baseados em palavras-chave */
  }
}

----------------------------------------

@media (max-width: 768px) {
  .tool-grid {
    grid-template-columns: minmax(0, 1fr) !important;
  }
}

----------------------------------------

@media (max-width: 768px) {
  .tool-container > .tool-grid {
    grid-template-columns: 1fr !important;
  }
}

----------------------------------------

@media (max-width: 480px) {
    .welcome-card, 
    #content-area > div {
        padding-left: 1.25rem !important;
        padding-right: 1.25rem !important;
    }
}

----------------------------------------

@media (max-width: 480px) {
    .welcome-card div[style*="grid-template-columns"] {
        grid-template-columns: 1fr !important;
    }
}

----------------------------------------

@media (max-width: 768px) {
    .welcome-grid {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
}

----------------------------------------

@media (max-width: 768px) {
  /* 1. O menu fica oculto por padrÃ£o fora da tela */
  #sidebar {
    position: fixed !important;
    top: 0;
    left: 0;
    height: 100vh;
    height: 100dvh; /* Altura dinÃ¢mica ideal para navegadores mobile */
    z-index: 1050;
    width: var(--sidebar-width) !important;
    transform: translateX(-100%) !important;
    transition: transform 0.3s ease-in-out;
    margin-left: 0 !important;
    box-shadow: none;
    
    /* === CORREÃ‡ÃƒO DO SCROLL === */
    overflow-y: auto !important;          /* Ativa a rolagem vertical prÃ³pria do menu */
    -webkit-overflow-scrolling: touch;    /* Suaviza o scroll em dispositivos iOS (Safari) */
    overscroll-behavior: contain;         /* Impede que o scroll puxe a pÃ¡gina de fundo */
  }

  /* 2. Quando aberto, ele desliza para a tela com sombra nÃ­tida */
  #app-container:not(.sidebar-hidden) #sidebar {
    transform: translateX(0) !important;
    box-shadow: 20px 0 50px rgba(0, 0, 0, 0.4);
  }

  /* 3. Trava o fundo da pÃ¡gina para o scroll nÃ£o vazar para o body */
  #app-container:not(.sidebar-hidden) {
    overflow: hidden;
    height: 100vh;
    height: 100dvh;
  }

  /* 4. Camada de fundo escuro sutil atrÃ¡s do menu para dar contraste */
  #app-container:not(.sidebar-hidden)::before {
    content: '';
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background-color: rgba(0, 0, 0, 0.4);
    z-index: 1040;
    pointer-events: auto; /* Garante que toque fora feche ou bloqueie o clique */
  }

  /* 5. ConteÃºdo principal ocupa 100% da largura sempre */
  #main-content {
    width: 100% !important;
    min-width: 0;
  }
}

----------------------------------------

@media (max-width: 520px) {
  .tool-container .rede-tabs {
    flex-direction: column !important;
    align-items: center !important; /* CORREÃ‡ÃƒO: align-items:stretch Ã© o valor padrÃ£o
                                      do flexbox. Sem essa linha, ao empilhar os botÃµes
                                      em coluna, cada .tab-btn esticava para ocupar 100%
                                      da largura do container â€” e o border-bottom (o
                                      "underline" colorido) esticava junto, cobrindo a
                                      tela toda em vez de sÃ³ o texto. Isso nÃ£o vinha de
                                      nenhuma duplicata antiga: era o comportamento
                                      padrÃ£o do flexbox em flex-direction:column, que
                                      esquecemos de neutralizar. */
    gap: 2px !important;
    padding: 0 !important;
  }

  .tool-container .rede-tabs .tab-btn {
    justify-content: center !important;
  }
}

----------------------------------------

